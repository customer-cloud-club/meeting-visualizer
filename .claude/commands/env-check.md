# 環境チェック

開発環境の設定を確認します。

## 実行

```bash
# Node.jsバージョン
node -v

# npmバージョン
npm -v

# 環境変数確認
cat .env | grep -v "^#" | grep -v "^$"

# 依存関係確認
npm ls --depth=0
```

## チェック項目

- [ ] Node.js 20以上
- [ ] 必要な環境変数が設定済み
- [ ] データベース接続可能
- [ ] 必要なCLIツールがインストール済み
