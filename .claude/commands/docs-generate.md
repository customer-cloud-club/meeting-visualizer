---
description: ドキュメント自動生成（CMD-021）
---

# ドキュメント自動生成

CCAGI SDK Phase 6 コマンド (CMD-021)

## ⚠️ SWMLワークフロー指示（必須）

このコマンドを実行する際、**必ず**以下のワークフローに従ってください：

```
メモリ使用量は、最大92%
そのために効率よく並列処理
必ず、必ず、必ず、miyabi のワークフローシステムに従って、タスク分解して計画、プランニングを行ってください。
このマスターIssue、このマスタータスクをタスク分解、分解バラバラにしちゃって、アサインできる状態でサブIssuesに分割してください。
分割したら、目的を達成するまで、作業をひたすら続けてください

完了したIssueはクローズしてください
```

**SWMLフロー**: θ₁ Understand → θ₂ Generate → θ₃ Allocate → θ₄ Execute → θ₅ Integrate → θ₆ Learn

---

プロジェクトのドキュメントを自動生成します。

## 生成対象

1. API リファレンス
2. コンポーネントドキュメント
3. 設定ファイルの説明
4. アーキテクチャ図

## 実行

documentation agentを起動して、以下を生成してください：

```bash
# TypeDoc でAPIドキュメント生成
npx typedoc --out docs/api src/

# または手動でREADME更新
```

生成されたドキュメントは`docs/`ディレクトリに配置されます。

---

## 実行時の指示（Claude向け）

このコマンドを実行する際、必ず以下のGitHub Issue連携を行ってください：

### Step 1: SSOT Issue・Phase 6 Issue取得

`.ccagi.yml` からIssue番号を取得：

```bash
SSOT_ISSUE=$(grep 'issue_number' .ccagi.yml 2>/dev/null | awk '{print $2}')
PHASE6_ISSUE=$(grep 'phase6' .ccagi.yml 2>/dev/null | awk '{print $2}')
```

### Step 2: Phase 6 Issue作成（存在しない場合）

Phase 6 Issueが存在しない場合、**必ず**作成：

```bash
if [ -z "$PHASE6_ISSUE" ] && [ -n "$SSOT_ISSUE" ]; then
  PHASE6_ISSUE=$(gh issue create \
    --title "📚 Phase 6: ドキュメント - #${SSOT_ISSUE}" \
    --body "$(cat <<EOF
親Issue: #${SSOT_ISSUE}

## 📚 Phase 6: Documentation

ドキュメント生成フェーズの作業を管理します。

## タスク

- [ ] APIリファレンス生成
- [ ] コンポーネントドキュメント生成
- [ ] 設定ファイル説明生成
- [ ] アーキテクチャ図更新
- [ ] README更新

## 生成ファイル

- docs/api/
- docs/components/
- docs/configuration/
- README.md

## 依存関係

- **依存元**: Phase 5 (Testing)
- **依存先**: Phase 7 (Deployment)

---
🤖 Generated by CCAGI SDK
EOF
)" \
    --label "phase:documentation,🤖 automated" | grep -oE '[0-9]+$')

  echo "Phase 6 Issue #${PHASE6_ISSUE} を作成しました"

  # .ccagi.yml に記録
  echo "  phase6: ${PHASE6_ISSUE}" >> .ccagi.yml

  # SSOT Issueにコメント
  gh issue comment ${SSOT_ISSUE} --body "## 📚 Phase 6: Documentation 開始

Phase 6 Issue: #${PHASE6_ISSUE}

開始時刻: $(date '+%Y-%m-%d %H:%M:%S')
"
fi
```

### Step 3: ドキュメント生成結果をPhase 6 Issueに報告

```bash
if [ -n "$PHASE6_ISSUE" ]; then
  gh issue comment ${PHASE6_ISSUE} --body "## 📚 ドキュメント生成結果

完了時刻: $(date '+%Y-%m-%d %H:%M:%S')

### 生成ファイル
- docs/api/
- docs/components/
- README.md
"
fi
```

### Step 4: Phase 6完了時

全てのドキュメント生成が完了したら：

```bash
# Phase 6 Issueをクローズ
gh issue close ${PHASE6_ISSUE} --comment "✅ Phase 6 完了 - ドキュメント生成が完了しました"

# SSOT Issueを更新
gh issue comment ${SSOT_ISSUE} --body "## ✅ Phase 6: Documentation 完了

完了時刻: $(date '+%Y-%m-%d %H:%M:%S')

### 生成ファイル
- docs/api/
- docs/components/
- README.md

### 次のステップ
\`/deploy\` を実行してPhase 7を開始
"
```

### Step 5: 完了報告

ユーザーに以下を報告：
- 生成されたドキュメント一覧
- **Phase 6 Issue URL**
- **SSOT Issue URL**
- 次のステップ

---

🤖 CCAGI SDK v6.21.5 - Phase 6: Documentation (CMD-021)
